#include "../../minishell.h"

// Will check each char if it is a special char such as:
// < > << >> $ "" '' "SPACE" ...
int check_char()
{

}

// Tokenize the input.
// Find 
int parser(t_main_data *data)
{
    
    return (0);
}
